[build-system]
requires = ["briefcase"]
build-backend = "briefcase.backend"

[tool.briefcase]
project_name = "My App"
bundle = "org.example"
version = "0.1.0"

[tool.briefcase.app.myapp]
formal_name = "My App"
sources = ["src/myapp"]